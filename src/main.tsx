import { createRoot } from "react-dom/client";
import App from "./App.tsx";
import "./index.css";

// Initialize app - Lovable Cloud environment
createRoot(document.getElementById("root")!).render(<App />);
